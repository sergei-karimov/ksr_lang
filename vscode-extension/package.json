{
  "name": "ksr-lang",
  "displayName": "Kestrel Language",
  "description": "Syntax highlighting and error reporting for the Kestrel language",
  "version": "0.1.0",
  "publisher": "ksr",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Programming Languages",
    "Debuggers"
  ],
  "activationEvents": [
    "onDebugResolve:ksr",
    "onDebugInitialConfigurations:ksr"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "ksr",
        "aliases": [
          "KSR",
          "ksr"
        ],
        "extensions": [
          ".ksr"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "ksr",
        "scopeName": "source.ksr",
        "path": "./syntaxes/ksr.tmLanguage.json"
      }
    ],
    "configuration": {
      "title": "Kestrel",
      "properties": {
        "ksr.executablePath": {
          "type": "string",
          "default": "kestrel",
          "description": "Path to the Kestrel executable used to start the Language Server. Defaults to 'kestrel' (must be on PATH); 'ksr' remains a compatibility alias."
        }
      }
    },
    "breakpoints": [
      { "language": "ksr" }
    ],
    "debuggers": [
      {
        "type": "ksr",
        "label": "Kestrel",
        "languages": ["ksr"],
        "configurationSnippets": [
          {
            "label": "Kestrel: Launch",
            "description": "Launch and debug a Kestrel project (requires C# extension)",
            "body": {
              "name": "Debug Kestrel",
              "type": "ksr",
              "request": "launch",
              "preLaunchTask": "build",
              "program": "^\"\\${workspaceFolder}/bin/Debug/net10.0/\\${workspaceFolderBasename}.dll\"",
              "args": [],
              "cwd": "^\"\\${workspaceFolder}\"",
              "console": "internalConsole",
              "stopAtEntry": false
            }
          }
        ]
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p .",
    "bundle": "esbuild src/extension.ts --bundle --outfile=out/extension.js --external:vscode --format=cjs --platform=node",
    "test:resolver": "npm run compile && node --test test/executableResolver.test.js",
    "watch": "npm run bundle -- --watch",
    "vscode:prepublish": "npm run bundle -- --minify",
    "package": "vsce package"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.80.0",
    "@vscode/vsce": "^2.24.0",
    "esbuild": "^0.27.4",
    "typescript": "^5.3.0"
  }
}
